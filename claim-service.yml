spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartsure_claim?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Sai@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

rabbitmq:
  queue:
    claim:
      name: smartsure.claim.queue
  routing:
    claim:
      key: smartsure.routing.claim

logging:
  level:
    com.smartsure.claim: DEBUG
    org.springframework.amqp: INFO
