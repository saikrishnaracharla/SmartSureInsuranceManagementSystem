spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartsure_admin?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Sai@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

rabbitmq:
  listener:
    simple:
      acknowledge-mode: auto

logging:
  level:
    com.smartsure.admin: DEBUG
    org.springframework.amqp: INFO
